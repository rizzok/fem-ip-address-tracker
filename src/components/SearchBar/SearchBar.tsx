export const SearchBar = () => {
  return (
    <form>
      <input
        type="text"
        name="searchbar"
        id="searchbar"
        placeholder="Search for any IP address or domain"
      />
      <button type="submit"></button>
    </form>
  )
}
